import File from '../model/File'
import * as path from 'path'

export default class FileWriter {
  public static createFile(outputDir: string, namespace: string, file: File): void {
    // TODO:
  }
}
